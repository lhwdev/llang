package com.lhwdev.llang.cst


/**
 * [CstCreateContext] can be originated from raw code source, serialized CST structure, or
 * existing other [CstCreateContext] (for cloning).
 */
interface CstCreateContext
